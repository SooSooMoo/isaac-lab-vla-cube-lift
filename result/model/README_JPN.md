# 現行モデル

[English](README_EMG.md)

[manifest.json](manifest.json)に、最終構成の4ファイルの元パスとSHA-256を記録しています。

- 基礎モデル
- 視覚アダプター
- 開閉補正
- XY減衰

基礎モデル単体では現行結果を再現しません。4つの構成要素すべてが必要です。

重みはPod更新時にコピーし、ハッシュを照合します。`.gitignore` は重みを通常の追加対象から除外するため、必要な重みがGitHubにすべて揃っているとは限りません。既存Podを使用する検証スクリプトは元実験パスを参照します。
